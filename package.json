{
  "name": "my-first-project",
  "version": "1.0.0",
  "description": "基于 Harness + OpenSpec + Superpowers 理念的 AI Coding 开发项目",
  "private": true,
  "scripts": {
    "openspec:init": "openspec init --tools qoder --profile core",
    "openspec:update": "openspec update",
    "openspec:list": "openspec list",
    "openspec:view": "openspec view",
    "openspec:validate": "openspec validate --all"
  },
  "devDependencies": {
    "@fission-ai/openspec": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
